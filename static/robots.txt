# robots.txt for Helix-Ed
# https://helix-ed.org/robots.txt
#
# Deliberately minimal. The previous version blocked /_app/, which is where
# SvelteKit emits every JS and CSS bundle — that stopped search engines from
# rendering the site at all. It also blocked /*.xml$ (the sitemap itself) and
# /*.json$ (SvelteKit's client-navigation payloads), and pointed the Sitemap
# directive at helix-ed.com, which does not host this site.

User-agent: *
Allow: /

Sitemap: https://helix-ed.org/sitemap.xml
